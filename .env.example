# Supabase – reemplazar con los valores de tu proyecto (Settings → API)
# Usar la clave "anon" / "public" (publishable), NUNCA la service_role (secret)
EXPO_PUBLIC_SUPABASE_URL=https://TU_PROJECT_REF.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=tu_anon_public_key

# URL base de la app (para "Compartir con el médico"). Local: http://localhost:8081 o http://TU_IP:8081
EXPO_PUBLIC_APP_URL=http://localhost:8081
