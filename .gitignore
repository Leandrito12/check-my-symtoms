# dependencies
node_modules/

# local env files (claves secretas - NUNCA subir)
.env
.env*.local
.env.development
.env.production
.env.*.local

# Build / export (no subir artefactos de build)
dist/
build/
.expo/
out/
web-build/

# IDE / editor (opcional: quitar si el equipo versiona .vscode)
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs y depuración
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Tests y cobertura
coverage/
.nyc_output/

# Android (claves y generados locales)
*.keystore
!debug.keystore
local.properties
.gradle/
android/app/build/
android/build/

# iOS (Pods y estado Xcode)
ios/Pods/
ios/build/
*.xcworkspace
!default.xcworkspace
*.xcuserstate
*.xcuserdatad

# Vercel (config local del CLI)
.vercel

# Certificados y claves (nunca subir)
*.pem
*.key
*.p12
*.jks

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli